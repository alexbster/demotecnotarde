import 'reflect-metadata';
import { expect } from 'chai';
import dotenv from 'dotenv';
dotenv.config();

describe('Pruebas de funcionamiento sobre tareas', (): void => {
  it('TODO: lo dejamos a su conciencia', async (): Promise<void> => {
    expect(1).to.equal(1);
  }).timeout(60000);
});